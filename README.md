# 罗慧婷-24306038-第二次人工智能编程作业

仓库链接: https://github.com/2039067412-code/homework

## 1. 任务拆解与 AI 协作策略

开发前我先自主拆分整体开发任务，遵循模块化、面向对象要求规划项目结构，仅在局部代码编写、故障排查、代码优化环节借助 AI 辅助开发，完整开发步骤如下：

步骤1：自主研读作业需求文档，手动拆分项目架构，确定项目拆分为student.py、exam_system.py、main.py三个独立文件，规划Student实体类与ExamSys业务类的整体结构，手写所有类、功能方法的空框架与功能注释，敲定六大功能的实现逻辑大纲，全程自主完成项目基础架构搭建。

步骤2：着手实现load_students学生信息读取功能时，发现本地 txt 文件制表符分隔格式、表头字段排布和最初设想的逗号切割逻辑不一致，自行核对文本内容、排查基础格式问题后，借助 AI 辅助分析文本结构适配方案，筛选AI给出的局部分割、字段映射代码片段，自主修改、适配、整合进读取函数主体，文件读取核心逻辑、容错判断均由本人自主把控实现。

步骤3：开发随机点名模块，自主梳理需要处理的三类异常需求（非数字输入、人数小于等于 0、人数超出总学生数），自主编写基础循环与异常捕获框架。在细化异常提示文案、优化异常分支逻辑、统一报错规范时，利用 AI 优化 try-except 局部分支代码，仅采纳局部优化片段，整体点名循环结构、业务逻辑完全沿用自己编写的代码。

步骤4：开发考场表、准考证生成功能，自主完成文件创建、路径拼接核心逻辑。开发中遇到文件夹重复创建、路径报错等边界问题，将完整报错日志提交AI分析报错诱因，参考AI优化后的局部 os 模块代码，手动修改文件生成逻辑，规避重复创建报错。所有基础功能开发完成后，自主全流程测试程序，发现菜单输入错误后重复刷屏、界面交互冗余的体验问题，借助AI分析循环结构缺陷，优化菜单交互逻辑，精简冗余代码、优化程序运行结构，提升程序交互合理性。

步骤5：项目收尾阶段，自主学习使用Git进行版本管控，针对Git提交混乱、文件忽略报错、代码缩进语法报错等问题，借助AI分析报错原因、给出局部修正方案，自主完成代码修正、规范版本提交、远程仓库推送，保证项目代码规范、版本记录清晰。

## 2. 核心 Prompt 迭代记录

**初代 Prompt**：帮忙优化随机点名的异常处理代码。

**AI 生成的问题/缺陷**：异常捕获全部统一在一个 except 内，无法区分三种不同输入错误，报错提示统一输出，不符合作业要求的分项错误提示规范，同时代码改动范围过大，直接重写整个点名函数，完全覆盖我自主编写的代码框架，破坏原有程序结构，无法直接使用。

**优化后的 Prompt (追问)**：仅对已有随机点名代码做局部优化，禁止重写整体循环结构，按照需求拆分三类异常分别编写提示语句：输入非数字、数值≤0、数值大于总人数，分条件单独输出指定报错文字，仅使用 python 内置 random、标准语法，只给出需要修改的局部代码片段，不要改动整体代码框架。

**优化后效果**：AI 严格遵循要求，仅输出异常分支判断的局部优化代码，成功区分三类输入错误提示，完全贴合作业规范，未改动我自主编写的核心循环与业务逻辑，我仅替换对应局部代码段即可完成功能优化，最大限度保留了自主开发内容。

**迭代补充Prompt**：Python菜单输入错误编号后会重复打印整段菜单，交互冗余，只优化循环判断局部逻辑，不改动原有菜单打印和功能执行代码，实现输错编号仅提示错误、不重复刷新菜单。

**迭代效果**：AI精准定位循环结构缺陷，给出局部逻辑优化方案，我手动修改代码后，彻底解决菜单刷屏问题，程序交互体验大幅优化，且完全保留原有功能逻辑。



## 3. Debug 与异常处理记录

**报错类型/漏洞现象1**：运行程序读取学生名单时报错ValueError: not enough values to unpack (expected 5, got 1)，文件读取分割字段数量不匹配，无法实例化学生对象，程序读取数据失败。

**解决过程**：出现报错后，我首先手动打开人工智能编程语言学生名单.txt查看原始内容，自主排查发现文档使用制表符分隔数据且首行存在中文表头，和代码原先逗号切割的逻辑冲突，但无法精准定位字段下标匹配问题。随后把完整报错信息+txt原始文本内容发送给AI，由AI分析报错核心诱因、给出分隔符替换、跳过表头、调整字段索引的局部修改方案，我对照方案手动修改load_students函数中的分割逻辑，把逗号分隔改为制表符分割、增加表头跳过逻辑、重新匹配学号/姓名/班级等字段下标，最终彻底修复文件读取异常，成功加载学生数据。

**报错类型/漏洞现象2**：代码修改后运行报IndentationError缩进错误，程序无法启动，提示函数定义后缺少缩进代码块。

**解决过程**：自主排查为代码粘贴后缩进混乱，不符合Python语法规范，借助AI分析Python缩进规则与报错位置，手动统一代码4空格缩进，修正语法漏洞，保证程序正常运行。

**报错类型/漏洞现象3**：程序交互体验漏洞，输入错误功能编号（非0-4数字）后，系统会重复打印完整菜单，界面冗余杂乱，用户体验差。

**解决过程**：自主发现交互缺陷后，借助AI分析循环逻辑问题，获取局部结构优化方案，手动重构菜单输入判断逻辑，优化循环执行顺序，实现输错编号仅提示错误信息，不重复打印菜单的效果，保留所有原有功能，优化程序交互逻辑。

**报错类型/漏洞现象4**：Git操作报错，文件被.gitignore忽略、仓库绑定重复、提交记录混乱，无法正常提交和推送代码。

**解决过程**：将Git报错信息提交AI分析，理解.gitignore忽略规则、远程仓库绑定逻辑，自主重置混乱版本记录，规范完成5次阶段性代码提交，成功将完整项目推送至GitHub远程仓库。



## 4. 人工代码审查 (Code Review)

选取随机点名函数核心代码，所有注释为本人手动编写，逐条理解代码运行逻辑：

```python

def random_roll_call(self):
    """功能4：随机不重复点名，异常捕获：非数字、<=0、超总人数"""
    # 获取学生列表总长度，得到全体学生数量
    total = len(self.student_list)
    print(f"当前总学生数量：{total}名")
    # 循环结构，持续接收输入直到输入内容合法
    while True:
        num_str = input("请输入需要点名的学生数量：").strip()
        try:
            # 尝试将输入字符串转为整型，非数字内容会触发ValueError进入异常捕获
            num = int(num_str)
            # 判断输入数字小于等于0的场景，输出指定错误提示，继续循环等待重新输入
            if num <= 0:
                print("[输入错误]点名人数必须大于0。")
                continue
            # 判断输入数字超过学生总数的场景，输出超限提示
            if num > total:
                print(f"[输入错误]点名人数({num})超过学生总人数({total}),请重新输入。")
                continue
            # random.sample实现无重复随机取样，从学生列表抽取指定数量学生
            pick_list = random.sample(self.student_list, num)
            print("本次随机点名结果：")
            # 遍历点名结果，序号从1开始格式化输出姓名+学号
            for index, s in enumerate(pick_list, start=1):
                print(f"{index}.{s.name} {s.sid}")
            # 输入合法，跳出while循环，结束点名功能
            break
        except ValueError:
            # 捕获字符串无法转数字的异常，输出对应报错信息
            print(f"[输入错误]invalid Literal for int() with base 10:'{num_str}'")
