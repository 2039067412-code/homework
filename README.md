# 罗慧婷-24306038-第二次人工智能编程作业

仓库链接: https://github.com/2039067412-code/homework

## 1. 任务拆解与 AI 协作策略

开发前我先自主拆分整体开发任务，遵循模块化、面向对象要求规划项目结构，仅在局部代码编写、故障排查、代码优化环节借助 AI 辅助开发，完整开发步骤如下：

步骤 1：自主研读作业需求文档，手动拆分项目架构，确定项目拆分为`student.py`、`exam\_system.py`、`main.py`三个独立文件，规划`Student`实体类与`ExamSys`业务类的整体结构，手写所有类、功能方法的空框架与功能注释，敲定六大功能的实现逻辑大纲。

步骤 2：着手实现`load\_students`学生信息读取功能时，发现本地 txt 文件分隔格式、字段排布和最初设想不一致，自行核对文本内容后，借助 AI 辅助分析文本结构，获取文本分割、字段映射的局部代码片段，由我筛选、修改后整合进读取函数主体，主体读取逻辑由自己把控。

步骤 3：开发随机点名模块，自主梳理需要处理的三类异常需求（非数字输入、人数小于等于 0、人数超出总学生数），在异常分支细化提示文案时逻辑不完善，利用 AI 优化 `try-except` 局部分支代码，只采纳优化片段，整体循环框架沿用自己编写的代码。

步骤 4：开发考场表、准考证生成功能，在文件夹创建、文件路径拼接环节遇到边界问题（文件夹重复创建报错），将报错信息发送 AI 分析原因，参考优化后的局部 os 模块代码，手动修改文件生成逻辑；所有功能完成后自主全流程测试，梳理 bug 并结合 AI 建议精简冗余代码、优化程序结构。

## 2. 核心 Prompt 迭代记录

**初代 Prompt**：帮忙优化随机点名的异常处理代码。



**AI 生成的问题 / 缺陷**：异常捕获全部统一在一个 except 内，无法区分三种不同输入错误，报错提示统一输出，不符合作业要求的分项错误提示规范，同时代码改动范围过大，直接重写整个点名函数，破坏我原本的代码框架。



**优化后的 Prompt (追问)**：仅对已有随机点名代码做局部优化，禁止重写整体循环结构，按照需求拆分三类异常分别编写提示语句：输入非数字、数值≤0、数值大于总人数，分条件单独输出指定报错文字，仅使用 python 内置 random、标准语法，只给出需要修改的代码片段。



优化后效果：AI 仅输出分支判断的局部优化代码，区分三类错误提示，贴合作业规范，我直接替换对应代码段即可。



## 3. Debug 与异常处理记录

**报错类型 / 漏洞现象**：运行程序读取学生名单时报错`ValueError: not enough values to unpack (expected 5, got 1)`，文件读取分割字段数量不匹配，无法实例化学生对象。



**解决过程**：出现报错后，我首先手动打开`人工智能编程语言学生名单.txt`查看原始内容，发现文档使用制表符分隔数据且首行存在中文表头，和代码原先逗号切割的逻辑冲突，但不清楚字段下标对应关系；随后把完整报错信息 + txt 原始文本内容发送给 AI，由 AI 分析报错诱因、给出分隔符替换、跳过表头、调整字段索引的局部修改方案，我对照方案手动修改`load\_students`函数中的分割逻辑，把逗号分隔改为制表符分割、增加表头跳过逻辑、重新匹配学号 / 姓名 / 班级等字段下标，最终修复文件读取异常。



## 4. 人工代码审查 (Code Review)

选取随机点名函数核心代码，所有注释为本人手动编写，逐条理解代码运行逻辑：

```python

def random_roll_call(self):
    """功能4：随机不重复点名，异常捕获：非数字、<=0、超总人数"""
    # 获取学生列表总长度，得到全体学生数量
    total = len(self.student_list)
    print(f"当前总学生数量：{total}名")
    # 循环结构，持续接收输入直到输入内容合法
    while True:
        num_str = input("请输入需要点名的学生数量：").strip()
        try:
            # 尝试将输入字符串转为整型，非数字内容会触发ValueError进入异常捕获
            num = int(num_str)
            # 判断输入数字小于等于0的场景，输出指定错误提示，继续循环等待重新输入
            if num <= 0:
                print("[输入错误]点名人数必须大于0。")
                continue
            # 判断输入数字超过学生总数的场景，输出超限提示
            if num > total:
                print(f"[输入错误]点名人数({num})超过学生总人数({total}),请重新输入。")
                continue
            # random.sample实现无重复随机取样，从学生列表抽取指定数量学生
            pick_list = random.sample(self.student_list, num)
            print("本次随机点名结果：")
            # 遍历点名结果，序号从1开始格式化输出姓名+学号
            for index, s in enumerate(pick_list, start=1):
                print(f"{index}.{s.name} {s.sid}")
            # 输入合法，跳出while循环，结束点名功能
            break
        except ValueError:
            # 捕获字符串无法转数字的异常，输出对应报错信息
            print(f"[输入错误]invalid Literal for int() with base 10:'{num_str}'")
